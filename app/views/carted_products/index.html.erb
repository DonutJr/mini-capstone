<h1>Cart</h1>

    <% @carted_products.each do |carted_product| %>
      <div class="col-md-4 col-sm-6 product-index-single">
        <div class="well">
          <h2>Name: <%= carted_product.product.name %></a></h2>
          <h3>Price: $<%= carted_product.product.price %></h3>
          <h3>Quantity: $<%= carted_product.quantity %></h3>

          <%= link_to "remove", "/carted_products/#{carted_product.id}", method: :delete  %>

          <br>
        </div>
      </div>
    <% end %>

<%= link_to "Buy Now", "/orders", method: :post, class: "btn btn-primary" %>

